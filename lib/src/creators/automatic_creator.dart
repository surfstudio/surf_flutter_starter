import 'package:surf_flutter_starter/src/config/config.dart';
import 'package:surf_flutter_starter/src/creators/creator.dart';

// TODO(taranov): implement AutomaticCreator.
/// Creates Project with given [Config] file path.
class AutomaticCreator extends Creator {
  @override
  Future<void> createByConfig(Config config) {
    // TODO(taranov): implement createByConfig
    throw UnimplementedError();
  }

  @override
  Future<Config> prepareConfig() {
    // TODO(taranov): implement prepareConfig
    throw UnimplementedError();
  }
}
